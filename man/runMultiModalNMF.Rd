% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runMultiModalNMF.R
\name{runMultiModalNMF}
\alias{runMultiModalNMF}
\title{Run multi-modal joint NMF on a SingleCellExperiment}
\usage{
runMultiModalNMF(
  x,
  k,
  assays = NULL,
  alt_exps = NULL,
  main_assay = "logcounts",
  alt_assay = "counts",
  modality_names = NULL,
  name = "MultiNMF",
  subset_rows = NULL,
  tol = 1e-05,
  maxit = 100,
  L1 = c(0, 0),
  L2 = c(0, 0),
  distribution = "mse",
  absorb_d = TRUE,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SingleCellExperiment or SpatialExperiment object}

\item{k}{Integer, number of shared factors}

\item{assays}{Character vector of >= 2 assay names from the main SCE.
Use this when modalities share the same feature space. Mutually exclusive
with \code{alt_exps}.}

\item{alt_exps}{Character vector of altExp names plus optionally the main
experiment. E.g., \code{c("main", "ATAC")} uses the main SCE's assay
as one modality and \code{altExp(x, "ATAC")} as another.}

\item{main_assay}{Character, assay to use from the main SCE when using
\code{alt_exps} (default "logcounts")}

\item{alt_assay}{Character, assay to use from each altExp (default "counts")}

\item{modality_names}{Character vector of short names for each modality.
Defaults to assay or altExp names.}

\item{name}{Character, name for the reducedDim slot (default "MultiNMF")}

\item{subset_rows}{Named list of per-modality feature subsets}

\item{tol}{Numeric, tolerance for convergence (default 1e-5)}

\item{maxit}{Integer, maximum iterations (default 100)}

\item{L1}{Numeric vector of length 2, L1 regularization (default c(0,0))}

\item{L2}{Numeric vector of length 2, L2 regularization (default c(0,0))}

\item{distribution}{Character, loss function (default "mse")}

\item{absorb_d}{Logical, whether to absorb diagonal scaling (default TRUE)}

\item{seed}{Integer, random seed for reproducibility}

\item{verbose}{Logical, whether to print progress (default TRUE)}

\item{...}{Additional arguments passed to \code{\link[RcppML]{factor_config}}}
}
\value{
The input object with multi-modal NMF results stored in:
  \itemize{
    \item \code{reducedDim(x, name)}: cells x k shared coefficient matrix
    \item \code{metadata(x)[[paste0(name, "_basis_", modality)]]}: per-modality
      basis matrices (features_i x k)
    \item \code{metadata(x)[[paste0(name, "_modalities")]]}: character vector
      of modality names
    \item \code{metadata(x)[[paste0(name, "_model")]]}: raw FactorNet result
  }
}
\description{
Performs joint NMF factorization across multiple modalities from the same
cells. Each modality gets its own feature loadings (W) while sharing a
common cell embedding (H). Uses \code{\link[RcppML]{factor_shared}}
internally.
}
\details{
Modalities can be specified as either:
\itemize{
  \item Multiple assays in the main SCE (same feature space, e.g., different
    normalization layers) via the \code{assays} parameter
  \item Alternative experiments (different feature spaces, e.g., RNA + ATAC)
    via the \code{alt_exps} parameter, where each altExp is a separate SCE
    stored via \code{\link[SingleCellExperiment]{altExp}}
}
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 100, ncells = 50)
sce <- logNormCounts(sce)

# Create altExp for second modality
atac <- SingleCellExperiment(list(
    counts = abs(matrix(rnorm(50 * ncol(sce)), 50, ncol(sce)))
))
rownames(atac) <- paste0("peak_", seq_len(50))
colnames(atac) <- colnames(sce)
altExp(sce, "ATAC") <- atac

sce <- runMultiModalNMF(sce, k = 5,
                        alt_exps = c("main", "ATAC"),
                        modality_names = c("RNA", "ATAC"))
getBasis(sce, "MultiNMF", modality = "RNA")

}
