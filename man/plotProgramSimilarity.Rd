% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotProgramSimilarity.R
\name{plotProgramSimilarity}
\alias{plotProgramSimilarity}
\title{Plot the program similarity matrix from an alignment}
\usage{
plotProgramSimilarity(
  alignment,
  order_by_match = TRUE,
  show_values = TRUE,
  digits = 2,
  highlight_matches = TRUE,
  highlight_color = "black",
  color_palette = c("#f7fbff", "#6baed6", "#08306b"),
  x_label = "Programs in x",
  y_label = "Programs in y"
)
}
\arguments{
\item{alignment}{A \code{programAlignment} object, the output of
\code{\link{alignPrograms}}.}

\item{order_by_match}{Logical, whether to reorder the programs of \code{y}
so matched pairs fall on the diagonal (default TRUE). Set FALSE to keep
the original program order.}

\item{show_values}{Logical, whether to print the similarity in each tile
(default TRUE).}

\item{digits}{Integer, number of decimal places for the printed values
(default 2).}

\item{highlight_matches}{Logical, whether to outline the matched pairs
(default TRUE).}

\item{highlight_color}{Character, outline color for matched pairs
(default "black").}

\item{color_palette}{Character vector of colors defining the fill gradient
from low to high similarity
(default c("#f7fbff", "#6baed6", "#08306b")).}

\item{x_label}{Character, axis label for the programs of \code{x}
(default "Programs in x").}

\item{y_label}{Character, axis label for the programs of \code{y}
(default "Programs in y").}
}
\value{
A ggplot object.
}
\description{
Draws the program-by-program similarity matrix produced by
\code{\link{alignPrograms}} as a heatmap. Programs in \code{y} are reordered
so that the pairs chosen by the bipartite matching lie on the diagonal: a
pair of runs that recovered the same programs shows a bright diagonal and a
dim off-diagonal, while off-diagonal brightness marks programs that split,
merged, or are specific to one run.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 200, ncells = 100)
sce <- logNormCounts(sce)
fit_a <- runNMFscape(sce, k = 4, seed = 1, verbose = FALSE)
fit_b <- runNMFscape(sce, k = 4, seed = 2, verbose = FALSE)

res <- alignPrograms(fit_a, fit_b)
plotProgramSimilarity(res)

# keep the original program order and drop the printed values
plotProgramSimilarity(res, order_by_match = FALSE, show_values = FALSE)

}
\seealso{
\code{\link{alignPrograms}}
}
