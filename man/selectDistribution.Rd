% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectDistribution.R
\name{selectDistribution}
\alias{selectDistribution}
\title{Select optimal NMF distribution for a dataset}
\usage{
selectDistribution(
  x,
  k = 10,
  assay = "logcounts",
  subset_row = NULL,
  distributions = c("mse", "gp", "nb"),
  criterion = "bic",
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SingleCellExperiment or SpatialExperiment object}

\item{k}{Integer, rank to use for comparison (default 10)}

\item{assay}{Character, which assay to use (default "logcounts")}

\item{subset_row}{Vector specifying which features to use}

\item{distributions}{Character vector of distributions to compare
(default c("mse", "gp", "nb"))}

\item{criterion}{Character, model selection criterion: "bic" or "aic"
(default "bic")}

\item{seed}{Integer, random seed for reproducibility}

\item{verbose}{Logical, whether to print progress (default TRUE)}

\item{...}{Additional arguments passed to
\code{\link[RcppML]{auto_nmf_distribution}}}
}
\value{
A list with components:
  \itemize{
    \item \code{best}: character, name of the best distribution
    \item \code{comparison}: data.frame with columns \code{distribution},
      \code{nll}, \code{df}, \code{aic}, \code{bic}, \code{selected}
  }
}
\description{
Evaluates multiple statistical distributions (loss functions) for NMF and
recommends the best one based on BIC or AIC. Wraps
\code{\link[RcppML]{auto_nmf_distribution}} with a SingleCellExperiment
interface.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 100, ncells = 50)
sce <- logNormCounts(sce)
result <- selectDistribution(sce, k = 5)
result$best
result$comparison

}
