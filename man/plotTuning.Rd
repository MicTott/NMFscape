% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTuning.R
\name{plotTuning}
\alias{plotTuning}
\title{Plot hyperparameter cross-validation results}
\usage{
plotTuning(tuning, show_se = TRUE, highlight_best = TRUE, top_n = 20, ...)
}
\arguments{
\item{tuning}{An \code{nmfscape_tuning} object from \code{\link{tuneNMF}}}

\item{show_se}{Logical, whether to draw standard-error bars where they apply
(default TRUE)}

\item{highlight_best}{Logical, whether to mark the best combination
(default TRUE)}

\item{top_n}{Integer, how many combinations to show in the ranked dot plot
used for three or more parameters (default 20)}

\item{...}{Additional arguments (currently unused)}
}
\value{
A ggplot object
}
\description{
Visualizes the output of \code{\link{tuneNMF}}. The layout adapts to how
many parameters were searched:
\itemize{
  \item One parameter: mean held-out loss against that parameter, with
    standard-error bars and the best value marked, in the style of
    \code{\link{plotRankSelection}}.
  \item Two parameters: a tile heatmap of mean held-out loss, with the best
    cell outlined.
  \item Three or more: a ranked dot plot of the best \code{top_n}
    combinations.
}
Deep-recipe results, which cross-validate one layer at a time, are faceted by
layer.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 80, ncells = 60)
sce <- logNormCounts(sce)

tuned <- tuneNMF(sce, params = list(k = c(2, 4, 6)), reps = 2,
                 verbose = FALSE)
plotTuning(tuned)

}
\seealso{
\code{\link{tuneNMF}}, \code{\link{plotRankSelection}}
}
