% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectRank.R
\name{selectRank}
\alias{selectRank}
\title{Select optimal NMF rank via cross-validation}
\usage{
selectRank(
  x,
  k = seq(2, 20),
  assay = "logcounts",
  subset_row = NULL,
  test_fraction = 0.2,
  distribution = "mse",
  n_reps = 1,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SingleCellExperiment or SpatialExperiment object}

\item{k}{Integer vector, ranks to evaluate (default 2:20)}

\item{assay}{Character, which assay to use (default "logcounts")}

\item{subset_row}{Vector specifying which features to use}

\item{test_fraction}{Numeric, fraction of data to hold out (default 0.2)}

\item{distribution}{Character, loss function (default "mse")}

\item{n_reps}{Integer, number of replicate runs with different seeds
(default 1). If > 1, results are aggregated with mean and sd.}

\item{seed}{Integer, random seed for reproducibility}

\item{verbose}{Logical, whether to print progress (default TRUE)}

\item{...}{Additional arguments passed to \code{\link[RcppML]{nmf}}}
}
\value{
A data.frame with columns:
  \itemize{
    \item \code{k}: rank tested
    \item \code{train_error}: training reconstruction error
    \item \code{test_error}: test (held-out) reconstruction error
    \item \code{sd_train_error}: standard deviation across reps (if n_reps > 1)
    \item \code{sd_test_error}: standard deviation across reps (if n_reps > 1)
  }
}
\description{
Runs NMF across a range of ranks using held-out data to estimate
reconstruction error. Wraps \code{\link[RcppML]{nmf}} with
\code{test_fraction > 0} and k as a vector.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 100, ncells = 50)
sce <- logNormCounts(sce)
cv_results <- selectRank(sce, k = 2:8)
cv_results

}
\seealso{
\code{\link{tuneNMF}}, which generalises this held-out
  cross-validation to any FactorNet recipe (deep, multi-modal, conditioned)
  and to any \code{\link[RcppML]{nmf_layer}} parameter, not just \code{k},
  and \code{\link{plotRankSelection}} to plot the result.
}
