% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assessNMF.R
\name{assessNMF}
\alias{assessNMF}
\title{Assess NMF embedding quality against known labels}
\usage{
assessNMF(
  x,
  name = "NMF",
  label_col,
  batch_col = NULL,
  min_class_size = 10,
  ...
)
}
\arguments{
\item{x}{A SingleCellExperiment or SpatialExperiment object with NMF results}

\item{name}{Character, name of the NMF result to assess (default "NMF")}

\item{label_col}{Character, column name in \code{colData(x)} containing
group labels (e.g., cell type annotations)}

\item{batch_col}{Character, optional column name for batch variable
(default NULL)}

\item{min_class_size}{Integer, minimum samples per class to include
(default 10)}

\item{...}{Additional arguments passed to \code{\link[RcppML]{assess}},
including \code{metrics}, \code{n_folds}, \code{classifiers},
\code{k_nn}, \code{seed}}
}
\value{
A list of class \code{nmf_assessment} with components:
  \itemize{
    \item \code{metrics}: named list of metric values (ari, nmi, silhouette,
      accuracy, f1, precision, recall, auroc per classifier)
    \item \code{classification}: data.frame of per-fold classification results
    \item \code{params}: list of assessment parameters
  }
}
\description{
Evaluates how well NMF factors separate known cell types or groups using
clustering metrics (ARI, NMI, silhouette) and cross-validated
classification. Wraps \code{\link[RcppML]{assess}} with a
SingleCellExperiment interface.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 100, ncells = 200)
sce <- logNormCounts(sce)
sce <- runNMFscape(sce, k = 5, verbose = FALSE)
sce$celltype <- sample(c("TypeA", "TypeB", "TypeC"), ncol(sce),
                       replace = TRUE)
result <- assessNMF(sce, label_col = "celltype", min_class_size = 5)
result$metrics$ari

}
