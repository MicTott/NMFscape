% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictNMF.R
\name{predictNMF}
\alias{predictNMF}
\title{Project new data onto an existing NMF model}
\usage{
predictNMF(
  new_data,
  reference,
  ref_name = "NMF",
  name = "NMF_projected",
  assay = "logcounts",
  absorb_d = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{new_data}{A SingleCellExperiment or SpatialExperiment with new samples}

\item{reference}{Either a SingleCellExperiment containing a stored NMF model
(from \code{\link{runNMFscape}}), or a raw RcppML \code{nmf} S4 object}

\item{ref_name}{Character, name of the NMF result in the reference SCE
(default "NMF"). Ignored if \code{reference} is a raw model.}

\item{name}{Character, name for the reducedDim slot in new_data
(default "NMF_projected")}

\item{assay}{Character, which assay to use from new_data (default "logcounts")}

\item{absorb_d}{Logical, whether to absorb diagonal scaling into the
projected coefficients (default TRUE)}

\item{verbose}{Logical, whether to print progress (default TRUE)}

\item{...}{Additional arguments passed to \code{RcppML::predict}}
}
\value{
The \code{new_data} object with projected NMF coefficients stored
  in \code{reducedDim(new_data, name)}
}
\description{
Projects a new SingleCellExperiment onto an NMF model learned from a
reference dataset. Handles feature alignment between the new data and
the model's basis matrix. Wraps \code{RcppML::predict} with
SCE integration.
}
\examples{
library(scuttle)
# Create reference and query datasets
ref_sce <- mockSCE(ngenes = 100, ncells = 50)
ref_sce <- logNormCounts(ref_sce)
ref_sce <- runNMFscape(ref_sce, k = 5, verbose = FALSE)

query_sce <- mockSCE(ngenes = 100, ncells = 30)
query_sce <- logNormCounts(query_sce)
query_sce <- predictNMF(query_sce, reference = ref_sce, verbose = FALSE)

}
