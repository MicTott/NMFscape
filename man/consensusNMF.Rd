% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensusNMF.R
\name{consensusNMF}
\alias{consensusNMF}
\title{Run consensus NMF on a SingleCellExperiment object}
\usage{
consensusNMF(
  x,
  k,
  assay = "logcounts",
  name = "cNMF",
  subset_row = NULL,
  n_runs = 50,
  absorb_d = TRUE,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SingleCellExperiment or SpatialExperiment object}

\item{k}{Integer, number of factors (rank)}

\item{assay}{Character, which assay to use (default "logcounts")}

\item{name}{Character, name for the reducedDim slot (default "cNMF")}

\item{subset_row}{Vector specifying which features to use}

\item{n_runs}{Integer, number of NMF replicates (default 50)}

\item{absorb_d}{Logical, whether to absorb diagonal scaling into W/H
(default TRUE)}

\item{seed}{Integer, random seed for reproducibility}

\item{verbose}{Logical, whether to print progress (default TRUE)}

\item{...}{Additional arguments passed to
\code{\link[RcppML]{consensus_nmf}}}
}
\value{
The input object with consensus NMF results stored in:
  \itemize{
    \item \code{reducedDim(x, name)}: cells x k coefficient matrix
      (from first replicate)
    \item \code{metadata(x)[[paste0(name, "_basis")]]}: genes x k basis
      matrix
    \item \code{metadata(x)[[paste0(name, "_consensus")]]}: full consensus
      result list (consensus matrix, clusters, cophenetic, hclust)
  }
}
\description{
Runs multiple NMF replicates and computes a consensus clustering to
identify stable sample groupings. Wraps
\code{\link[RcppML]{consensus_nmf}} with a SingleCellExperiment interface.
}
\examples{
library(scuttle)
sce <- mockSCE(ngenes = 100, ncells = 50)
sce <- logNormCounts(sce)
sce <- consensusNMF(sce, k = 3, n_runs = 10)

}
