% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichPrograms.R
\name{plotProgramEnrichment}
\alias{plotProgramEnrichment}
\title{Dot plot of program enrichment results}
\usage{
plotProgramEnrichment(enrichment, top_n = 5, padj_cutoff = 0.05, facet = FALSE)
}
\arguments{
\item{enrichment}{A data.frame returned by \code{enrichPrograms()}}

\item{top_n}{Integer, number of gene sets to show per program (default 5)}

\item{padj_cutoff}{Numeric, keep only gene sets with an adjusted p-value at
or below this (default 0.05)}

\item{facet}{Logical, give each program its own panel (default FALSE)}
}
\value{
A ggplot object
}
\description{
Plots the top gene sets per program from \code{enrichPrograms()}. Point size
is the adjusted p-value on a -log10 scale and colour is the normalized
enrichment score (GSEA) or the odds ratio (over-representation).
}
\examples{
library(scuttle)
set.seed(1)
sce <- logNormCounts(mockSCE(ngenes = 200, ncells = 100))
sce <- runNMFscape(sce, k = 3, verbose = FALSE)

genes <- rownames(sce)
gene_sets <- list(SET_A = genes[1:30], SET_B = genes[31:60])
res <- enrichPrograms(sce, gene_sets, min_size = 5)
plotProgramEnrichment(res, padj_cutoff = 1)
}
